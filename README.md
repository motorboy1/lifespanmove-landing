# LifespanMove — Landing

Public landing page for **LifespanMove** (whole-lifespan movement-measurement SaaS).

- Source of truth: the private `motorboy1/lifespan-move` repo (`apps/landing/index.html`).
- This repo holds only the published landing (`index.html`) + the GitHub Pages workflow.
- Self-contained static page (Pretendard via CDN), with a ko/en language selector.

Live site: published via GitHub Pages (Settings → Pages → Source: GitHub Actions).

> Marketing copy mirrors the project's approved positioning. Provisional pre-launch — the formal scope of measurement metrics and clinical interpretation is announced as of launch.

🗿 MoAI
